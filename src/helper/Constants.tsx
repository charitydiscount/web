export enum Constants {
    USER = "user",
    JW_TOKEN = "jwToken",
    AUTHORIZATION_HEADER = 'Authorization',
}

export enum ButtonType {
    SUBMIT = "submit",
    BUTTON = "button"
}

export enum InputType {
    TEXT = "text",
    PASSWORD = "password",
    RADIO = "radio",
    CHECKBOX = "checkbox",
    DATE = "date",
    TIME = "time"
}
